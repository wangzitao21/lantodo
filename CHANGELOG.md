# 更新记录

## v1.0.1 · 终端响应与启动优化 / build 19（2026-09-12）

- Android 列表复用卡片、字体和未变化的附件视图，滚动与刷新减少控件分配；复用时重新绑定当前记录，清理旧滑动状态。
- 完成、星标、颜色和删除按顺序在后台保存，输入过程的草稿自动保存移出界面线程；发送、退出和恢复草稿仍等待既有写入完成，防止旧草稿回写。
- 内容变更后的界面合并等待由 80ms 缩短为 16ms；Windows 无变化时跳过清单与附件遍历，设置窗口按需创建。
- Android 网络准备移到后台；相同持久身份的附加空间复用证书句柄，减少重复导入；缓存设备显示名称并在昵称变化时失效。
- 移除同步中未使用的全历史附件集合构造，保留现有附件授权校验。数据库、备份格式和同步协议保持兼容。
- 对外继续显示 v1.0.1，Android 安装序号递增至 19，沿用原发布签名。

## v1.0.1 · 手机快速打开与重连 / build 18（2026-09-12）

- Android 直接展示首页和输入框，取消应用图标等待；历史载入、网络连接在后台启动，不再挡住记录。
- 数据库尚未打开时也可发送文字，先持久保存再自动写入清单；重启重放不重复、不复活已删除记录，旧草稿独立保留。
- 回到前台、网络地址变化和手动同步立即触发重连；多个已知地址错峰并发尝试，避免旧地址的握手超时挡住可用地址。
- 先发送本机更新再下载历史；自动同步中出现新文字时，让附件传输在分块之间让出下一轮，随后断点续传。
- Android 启用发布版裁剪与配置引导 AOT，减少安装包和启动开销；减少列表刷新前的历史、附件扫描及启动时重复哈希校验。
- 开启后台同步时，发送后提供最长 15 秒的 CPU 保持运行窗口，确认同步后提前释放；沿用原签名覆盖升级，安装序号递增至 18。

## v1.0.1 · 可靠性与轻量化修订 / build 17（2026-09-11）

- 修复备份恢复失败提前传播附件失效、本机无关待下载文件阻止恢复、占用附件导致已提交操作误报失败。
- 备份与上传前校验原件哈希；可靠备份可修复同大小损坏；临时不可读或损坏不再当作手动删除传播。
- 当前记录、历史和附件引用建立内存索引，清理仅处理失去引用的原件；后台延迟重试占用文件。
- Windows 虚拟列表与局部更新，Android 使用系统 ListView 按需创建卡片；增加标题、备注、文件名搜索。
- 两端保留输入与详情草稿，发送和移除草稿原子提交；旧草稿遇到远端修改时另存，避免覆盖。
- Windows 支持 Shift+Enter 换行；Android 日期与时间使用系统选择器；删除提供短时撤销，清空回收站原子提交。
- 同步等待复用认证连接，保留旧客户端兼容；状态明确显示待上线设备和同步诊断。
- Android 前后台分级发现与附件巡检，后台多播锁改为短窗口；允许显式选择移动数据，并支持无互联网本地 Wi-Fi。
- NAS 首次启动即要求令牌，默认在数据目录生成，保留现有配置；修复发现缓存过期清理。
- 保持 v1.0.1 展示版本与现有数据库、备份格式，Android 安装序号递增至 17。


## v1.0.1 · Windows 背景色菜单修复

- 补全自定义菜单模板的子菜单弹出层，修复右键“背景色”无法展开颜色选项的问题。
- 显示子菜单箭头、颜色预览和当前选中标记；新增实际展开子菜单并逐项选色、恢复默认、检查保存和卡片刷新的回归测试。
- 展示版本仍为 1.0.1，本次仅需更新 Windows 程序。

## v1.0.1 · 同步状态与消息交互（2026-09-11）

- 首页只显示本机暂未同步 / 已同步到最新；成员在线状态来自实际认证连接，绿点在线、红点当前不在线。
- 两端支持直接彻底删除、星标置顶与六种背景色选择；终端名移至消息底部，编辑与备份保留样式。
- Windows 移除底部操作提示，图片支持缩放和拖动，支持语音播放。
- Android 保留返回后的列表位置、修复返回按钮对齐、设置切换不再重建页面，系统返回从其他页回清单。
- Android 新增按住录音、松开发送、上滑取消（最长 60 秒），离开界面释放麦克风，两端同步与播放。
- 空闲同步采用事件等待，减少后台轮询与附件扫描，移除持续 CPU 唤醒锁，无本地网络时暂停连接。
- 展示版本仍为 1.0.1，Android 安装序号为 16，需全端更新以识别消息样式字段。

## v1.0.1 · 网络空间共用内容（2026-09-11）

- 手机首页移除空间栏；“待确认”仅在存在冲突时显示，解决后隐藏。
- 空间仅管理设备连接，全部清单、历史、回收站、草稿和附件共用；多网络成员设备可接力同步，成员权限仍分别校验。
- 自动合并旧版各空间内容、附件与失效状态，幂等重试并保留真实冲突。删除网络连接不再删除内容。
- Windows ZIP 只有一个 LanTodo-v1.0.1-Windows 顶层文件夹；更新启动验证以检查解压结构。
- 对外版本保持 1.0.1，Android 内部安装序号递增为 15，沿用原签名。

## v1.0.1 · 空间删除与统一样式（2026-09-11）

- 回收站恢复普通灰白页面和白色消息卡片，粉色仅用于按钮。
- Windows、Android、NAS 均新增“删除此空间”：退出并清除本机内容，自动选择剩余空间，最后一个删除后回到空白空间；退出证明保留以通知离线成员，支持新授权码重新加入。
- 统一设置、空间、备份、编辑和历史界面的配色、圆角与卡片；Windows 展开项和下拉框、Android 对话框主题及网页确认弹窗与主界面保持一致。
- 对外版本仍为 1.0.1，Android 安装序号为 14，沿用原发布签名。

## v1.0.1 · 多空间与回收站修复（2026-09-11）

- 支持多个独立命名空间、切换查看、共同网络入口与并行后台同步；三端空间设置重新整理。
- 移除今日入口，缩小 Windows 标题与清单间距，增加回收站入口、恢复和全部清除。
- Android 分享不再叠加 EXTRA_STREAM 与 ClipData 导出的副本。
- 原件按名称统一平铺，本地删除作为失效记录传播，不再自动修补；主动重新上传使用新引用。
- 版本仍为 1.0.1，Android 安装序号递增为 13，沿用原签名。

## v1.0.1 · 2026-09-10

- v1.0.1 修订：消息编辑与历史窗口支持 Esc；Windows 数据库固定随 EXE，附件使用原文件名与扩展名，删除后清理且不从历史重新同步。
- Android 新增系统单个/多个文件分享入口，修复输入框对齐，合并日期控件、右对齐待办数并隐藏滚动条。
- NAS 默认管理端口 42000，首次直接进入后设置令牌并记住登录；移除顶部历史统计，已解绑设备可删除记录。

- Android 长按消息操作菜单、向左滑出红色垃圾桶，继续左滑删除，滑回取消；多行输入、独立矢量加号和自适应宽度发送按钮，键盘与图片/文件面板切换，支持多选附件。
- Windows 支持粘贴、拖拽和加号选择原图片、文件、文件夹；品牌图文居中对齐，移除左下角两句提示，单击消息即可编辑，备份页可直接打开附件目录。
- 两端显示最后修改设备，可在设置关闭；图片预览与原件保存。
- 附件使用独立内容寻址存储、256 KiB 分块和断点续传，SHA-256 验证完整性；文件夹使用不压缩 ZIP。
- 完整历史备份与资料迁移包含原件；旧文本消息哈希和旧备份兼容。
- Docker 增加管理网页和独立管理令牌，支持绑定状态、授权码作废/删除记录、NAS 配对、同步与备份。
- 增加附件/授权码回归和管理页鉴权测试；说明现有直连与 NAS 共用一套同步引擎。

.NET 版本与展示版本为 1.0.1，Android 安装序号为 9；使用附件前请一起升级客户端与 NAS。

## v1.0 · 2026-09-10

首次公开发布准备版本，提供 Windows / Android 客户端及可选 NAS 同步服务。

- 新增 .NET NAS 服务、Dockerfile / Compose、独立数据与备份卷、同用户本地管理命令和每日历史快照。
- Windows / Android 支持 NAS 地址配对、多节点配置、暂停/恢复与各节点状态；原局域网直连保持默认启用。
- 固定节点连接支持 IPv4 / IPv6 / DNS、TLS 变化长轮询、离线补同步、独立退避和双 NAS 中转，继续保留删除与并发冲突。
- 新增多节点核心回归、NAS 进程恢复测试与 Linux / Docker CI，部署说明见 `docs/nas.md`。

- 手机可分别连接两个不互联的 NAS，往返携带同一份清单；也支持 NAS 直接互联中转。

- SQLite 本机存储、完整历史、删除与冲突保留、事务合并恢复。
- 首次配对、双向 TLS 局域网同步、设备发现与自动重连。
- Windows 托盘运行、便携数据库位置迁移、设备与备份窗口按 Esc 返回。
- Android 原生清单、可选后台同步、启动与自适应图标。
- 统一青绿色 L / 勾形图标，提供可维护 SVG 及平台资源生成脚本。
- 明确 ZIP + JSON 数据备份的范围，保持旧格式兼容。
- GPL-3.0-only 许可证、第三方声明和完整构建、使用、部署说明。

应用展示版本统一为 v1.0；.NET 数值版本为 1.0.0，Android 安装序号为 8，以支持已有开发版覆盖升级。数据库、版本记录、同步协议和备份格式继续保持独立的 v1 兼容约定。实际完成的验证与尚未验证的环境见 [验证说明](docs/acceptance.md)。


### v1.0.1 交互与便携修订（版本号不变）

- Windows 改为目录式便携 ZIP；标题右侧显示数量，日期筛选合并日期/星期，已完成页用全部删除替代详细新建；移除侧栏“我的空间”，缩小并对齐加号。
- 设置窗口复用；两端合并刷新、跳过未变列表、缓存设备来源偏好；缩略图后台限流解码并缓存。
- Windows 附件右键支持默认打开、定位文件夹、另存为；外部删除原件显示失效。
- Android 滑动未过半自动回弹，分享/选择附件按 URI 和内容去重，原件复制与哈希改为一次顺序读取。Android 内部升级码增加，显示版本仍为 1.0.1。
- 统一设备与同步入口；NAS 添加设备引导和配对记录关联，真实连接状态与绑定状态分开显示。
- 设备昵称可由终端修改自身、NAS 修改所有绑定设备，随可信同步传播且不改写历史。
- 取消绑定与删除绑定通过固定证书验证后的通知更新终端，离线终端下次连接清理记录；支持重新绑定。
- 三端冲突卡片直接选择最终内容，保留删除和客户端自由合并入口。

### v1.0.1 统一空间互联（2026-09-11，版本号不变）

- Windows、Android、NAS 改用同一空间与加入入口，任何成员可生成单设备、五分钟授权码和二维码；Android 增加相机扫码和相册识别，Windows 支持二维码图片读取。
- 签名成员因果日志自动传播加入与移除决定，成员独立发现、连接、转发与补同步；NAS 成为可选成员，邀请设备离线不再阻断其他成员互通。
- 移除从所有成员列表收敛消失，保留内部移除证明与本机数据；新的邀请可重新加入。旧版配对须明确升级，不静默扩大信任。
- 三端移除 LAN / NAS 两套选择，地址和核对间隔进入高级设置；加入前明确确认共享本机清单、附件与历史。
- 沿用 v1.0.1 与原 Android 发布签名，Android 内部安装序号增至 12。
